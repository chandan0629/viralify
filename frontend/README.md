````markdown\n```markdown\n# frontend — Song Virality Game (Vite + React)\n\nThis folder contains a Vite + React frontend that provides a gamified UI for the Song Virality Predictor. The app includes a fallback frontend predictor so it works without a backend API.\n\nLocal dev:\n\n1. cd frontend\n2. npm install\n3. npm run dev\n\nBuild for production (Vercel friendly):\n\n1. cd frontend\n2. npm run build\n\nDeploy: Vercel will detect the project (root `frontend/`) and run `npm run build` by default. Set the output directory to `dist`.\n```\n````